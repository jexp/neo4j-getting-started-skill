[project]
name = "neo4j-getting-started-skill-tests"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "neo4j>=6.0.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
